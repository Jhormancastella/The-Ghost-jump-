{
  "name": "the-ghost-jump",
  "version": "1.0.0",
  "description": "The Ghost Jump Game",
  "scripts": {
    "start": "serve .",
    "android-sync": "npx cap sync android",
    "android-open": "npx cap open android",
    "exe-build": "cd electron && npm install && npm run build"
  },
  "devDependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/cli": "^5.0.0",
    "@capacitor/core": "^5.0.0",
    "serve": "^14.2.0"
  }
}